name: daily-rollup-phase2

on:
  schedule:
    - cron: "10 9 * * *"
  workflow_dispatch:
    inputs:
      day:
        description: "Optional day (YYYY-MM-DD or YYYYMMDD). Leave blank to auto-detect."
        required: false
        default: ""

permissions:
  contents: write

concurrency:
  group: daily-rollup-phase2
  cancel-in-progress: false

jobs:
  rollup:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout data branch
        uses: actions/checkout@v4
        with:
          ref: data
          fetch-depth: 0

      - name: Configure git identity
        shell: bash
        run: |
          set -euo pipefail
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@users.noreply.github.com"

      # Bring code from main into this workspace WITHOUT merging branches
      - name: Bring pipeline code from main (no merge)
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main --depth=1
          git checkout origin/main -- src scripts ml requirements.txt pyproject.toml || true

          echo "---- verify filter_black_frames.py exists ----"
          ls -la scripts/pipeline/filter_black_frames.py

          echo "---- verify Phase2 runner exists ----"
          ls -la scripts/pipeline/run_sample_pipeline.py

          echo "---- verify infer script exists ----"
          ls -la ml/infer_unet_masks.py

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -e .

      - name: Resolve DAY + input roots
        id: resolve
        shell: bash
        run: |
          set -euo pipefail

          RAWROOT="data/raw"
          INPUT_DAY="${{ github.event.inputs.day }}"

          norm_day() {
            [[ "$1" =~ ^[0-9]{8}$ ]] && echo "${1:0:4}-${1:4:2}-${1:6:2}" || echo "$1"
          }

          [[ -d "$RAWROOT" ]] || { echo "ERROR: $RAWROOT missing on data branch"; exit 1; }

          mapfile -t DAYS < <(find "$RAWROOT" -maxdepth 1 -mindepth 1 -type d -name "????-??-??" -printf "%f|%p\n" | sort)

          if [[ ${#DAYS[@]} -eq 0 ]]; then
            echo "ERROR: No YYYY-MM-DD folders under $RAWROOT"
            ls -la "$RAWROOT" || true
            exit 1
          fi

          if [[ -n "$INPUT_DAY" ]]; then
            DAY="$(norm_day "$INPUT_DAY")"
            DAYDIR="$(printf '%s\n' "${DAYS[@]}" | awk -F'|' -v d="$DAY" '$1==d {print $2}' | tail -n 1)"
            [[ -n "$DAYDIR" ]] || { echo "ERROR: day '$DAY' not found under $RAWROOT"; exit 1; }
          else
            LAST="${DAYS[-1]}"
            DAY="${LAST%%|*}"
            DAYDIR="${LAST#*|}"
          fi

          if [[ -d "$DAYDIR/latest" ]]; then
            INROOT="$DAYDIR/latest"
          elif [[ -d "$DAYDIR/sequences" ]]; then
            INROOT="$DAYDIR/sequences"
          else
            echo "ERROR: Neither latest/ nor sequences/ exists in $DAYDIR"
            ls -la "$DAYDIR" || true
            exit 1
          fi

          OUTMASTER="$DAYDIR/master"
          OUTFRAMES="$OUTMASTER/frames"
          OUTDIR="out/$DAY/daily"

          echo "day=$DAY" >> "$GITHUB_OUTPUT"
          echo "daydir=$DAYDIR" >> "$GITHUB_OUTPUT"
          echo "inroot=$INROOT" >> "$GITHUB_OUTPUT"
          echo "outmaster=$OUTMASTER" >> "$GITHUB_OUTPUT"
          echo "outframes=$OUTFRAMES" >> "$GITHUB_OUTPUT"
          echo "outdir=$OUTDIR" >> "$GITHUB_OUTPUT"

      - name: Filter black frames (pre-stage)
        id: filter
        shell: bash
        run: |
          set -euo pipefail

          INROOT="${{ steps.resolve.outputs.inroot }}"
          DAYDIR="${{ steps.resolve.outputs.daydir }}"

          CLEANROOT="$DAYDIR/filtered"
          QUAR="$DAYDIR/rejects/black"

          mkdir -p "$CLEANROOT" "$QUAR"

          python scripts/pipeline/filter_black_frames.py \
            --inputs "$INROOT" \
            --outdir "$CLEANROOT" \
            --quarantine "$QUAR" \
            --mean-max 6 \
            --std-max 6 \
            --mode copy

          echo "cleanroot=$CLEANROOT" >> "$GITHUB_OUTPUT"

      - name: Stage frames by copying CLEANROOT -> master/frames
        shell: bash
        run: |
          set -euo pipefail

          INROOT="${{ steps.filter.outputs.cleanroot }}"
          OUTFRAMES="${{ steps.resolve.outputs.outframes }}"

          mkdir -p "$OUTFRAMES"

          shopt -s globstar nullglob
          COPIED=0
          for f in "$INROOT"/**/*.png "$INROOT"/*.png; do
            if [[ -f "$f" ]]; then
              cp -f "$f" "$OUTFRAMES/"
              COPIED=$((COPIED+1))
            fi
          done

          echo "Copied $COPIED png files into $OUTFRAMES"

          if [[ "$COPIED" -eq 0 ]]; then
            echo "ERROR: No PNG frames found under cleaned root: $INROOT"
            exit 1
          fi

      - name: Run Phase 2
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "${{ steps.resolve.outputs.outdir }}"

          RAWDIR="${{ steps.resolve.outputs.outframes }}"

          python scripts/pipeline/run_sample_pipeline.py \
            --raw-dir "$RAWDIR" \
            --outdir "${{ steps.resolve.outputs.outdir }}" \
            --save-overlays

      # -------------------------
      # Phase 3: U-Net inference (MATCH LOCAL BEHAVIOR)
      # We create grayscale copies of frames before running ml/infer_unet_masks.py
      # Threshold: 0.3
      # Output: out/$DAY/phase3_unet_v2_thr03
      # -------------------------

      - name: Resolve U-Net model checkpoint
        id: model
        shell: bash
        run: |
          set -euo pipefail

          CANON="out/models/unet_cloudmask_latest.pt"
          if [[ -f "$CANON" ]]; then
            echo "model_path=$CANON" >> "$GITHUB_OUTPUT"
            echo "Using canonical: $CANON"
            exit 0
          fi

          echo "Canonical missing: $CANON"
          echo "Searching for newest *.pt under out/models..."
          mapfile -t CANDS < <(find out/models -type f -name "*.pt" -printf "%T@|%p\n" | sort -nr | head -n 1 | cut -d'|' -f2-)

          if [[ ${#CANDS[@]} -eq 0 ]]; then
            echo "ERROR: No model .pt found under out/models in this checkout."
            echo "Fix: commit a model to data branch at out/models/unet_cloudmask_latest.pt"
            exit 1
          fi

          echo "model_path=${CANDS[0]}" >> "$GITHUB_OUTPUT"
          echo "Using newest: ${CANDS[0]}"

      - name: Phase 3 prep — create grayscale frames (to match local)
        id: phase3_prep
        shell: bash
        run: |
          set -euo pipefail

          DAY="${{ steps.resolve.outputs.day }}"
          FRAMES="${{ steps.resolve.outputs.outframes }}"
          GRAYDIR="out/${DAY}/phase3_unet_v2_thr03/frames_gray"

          echo "DAY=$DAY"
          echo "FRAMES=$FRAMES"
          echo "GRAYDIR=$GRAYDIR"

          mkdir -p "$GRAYDIR"

          python - << 'PY'
          from pathlib import Path
          from PIL import Image

          frames = Path(r"${{ steps.resolve.outputs.outframes }}")
          graydir = Path(r"out/${{ steps.resolve.outputs.day }}/phase3_unet_v2_thr03/frames_gray")
          graydir.mkdir(parents=True, exist_ok=True)

          # copy/convert all pngs (flat folder) to grayscale "L"
          files = sorted(frames.glob("*.png"))
          print("Found frames:", len(files))

          if not files:
            raise SystemExit(f"ERROR: No PNGs in {frames}")

          for f in files:
            img = Image.open(f).convert("L")  # <- key: 1-channel grayscale
            out = graydir / f.name
            img.save(out)

          # quick sanity: show one sample stats
          import numpy as np
          a = np.array(Image.open(graydir / files[0].name))
          print("Sample gray:", files[0].name, "shape", a.shape, "min/max", int(a.min()), int(a.max()))
          PY

          echo "graydir=$GRAYDIR" >> "$GITHUB_OUTPUT"

      - name: Run Phase 3 (U-Net inference) using ml/infer_unet_masks.py @ thr=0.3 (on grayscale frames)
        id: phase3
        shell: bash
        run: |
          set -euo pipefail

          DAY="${{ steps.resolve.outputs.day }}"
          FRAMES_GRAY="${{ steps.phase3_prep.outputs.graydir }}"
          MODEL_PATH="${{ steps.model.outputs.model_path }}"
          OUTDIR="out/${DAY}/phase3_unet_v2_thr03"

          echo "DAY=$DAY"
          echo "FRAMES_GRAY=$FRAMES_GRAY"
          echo "MODEL_PATH=$MODEL_PATH"
          echo "OUTDIR=$OUTDIR"

          ls -la "$MODEL_PATH" || true
          mkdir -p "$OUTDIR"

          # This now feeds 1-channel (grayscale) images via PNGs saved in frames_gray
          python ml/infer_unet_masks.py \
            --frames "$FRAMES_GRAY" \
            --outdir "$OUTDIR" \
            --ckpt "$MODEL_PATH" \
            --threshold 0.3 \
            --image-size 256 \
            --device cpu

          echo "---- debug: check masks_not_empty (white pixel %) ----"
          python - << 'PY'
          from pathlib import Path
          import numpy as np
          from PIL import Image

          p = Path("out") / Path(r"${{ steps.resolve.outputs.day }}") / "phase3_unet_v2_thr03" / "masks_unet"
          files = sorted(p.glob("*.png"))
          print("masks_unet:", str(p))
          print("count:", len(files))

          if not files:
            raise SystemExit("ERROR: No masks written")

          for f in files[:20]:
            a = np.array(Image.open(f))
            white = float((a > 0).mean()) * 100.0
            print(f.name, "shape", a.shape, "min/max", int(a.min()), int(a.max()), "white%:", round(white, 4))
          PY

      # -------------------------
      # TEMP DEBUG: Inspect Phase 3 inputs + model behavior
      # (REMOVE after verification)
      # -------------------------
      - name: DEBUG Phase 3 — inspect frames + model outputs
        shell: bash
        run: |
          set -euo pipefail

          python - << 'PY'
          import torch
          import numpy as np
          from pathlib import Path
          from PIL import Image

          frames = Path("${{ steps.resolve.outputs.outframes }}")
          model_path = Path("out/models/unet_cloudmask_latest.pt")

          print("DEBUG Phase 3")
          print("Frames dir:", frames)
          print("Model path:", model_path)
          print("Model exists:", model_path.exists())

          imgs = sorted(frames.glob("*.png"))
          print("Frame count:", len(imgs))

          if not imgs:
              raise RuntimeError("No frames found for Phase 3 debug")

          img = imgs[0]
          print("Sample frame:", img.name)

          im = np.array(Image.open(img).convert("RGB"), dtype=np.float32) / 255.0
          x = torch.from_numpy(im).permute(2,0,1).unsqueeze(0)

          print("Input tensor stats:")
          print("  shape:", tuple(x.shape))
          print("  min:", float(x.min()))
          print("  max:", float(x.max()))
          print("  mean:", float(x.mean()))

          state = torch.load(model_path, map_location="cpu")
          print("Checkpoint type:", type(state))

          if isinstance(state, dict) and "state_dict" in state:
              print("Checkpoint contains state_dict")

          print("DEBUG COMPLETE")
          PY




      - name: Upload Phase 3 outputs (artifact)
        uses: actions/upload-artifact@v4
        with:
          name: phase3_unet_${{ steps.resolve.outputs.day }}
          path: out/${{ steps.resolve.outputs.day }}/phase3_unet_v2_thr03

      - name: Commit results to data
        shell: bash
        run: |
          set -euo pipefail
          git add -f data/raw out || true
          git commit -m "daily rollup + phase2 + phase3(thr=0.3): ${{ steps.resolve.outputs.day }}" || echo "No changes"
          git push origin data
