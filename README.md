# himawari-ml

**Automated cloud segmentation and temporal forecasting from Himawari-9 geostationary satellite imagery.**

Built by [Evan Jamison](https://github.com/evanjamison) · Python · PyTorch · GitHub Actions

---

## Overview

This project builds a fully automated pipeline that ingests live satellite imagery from the NICT Himawari Real-time Web every hour, generates cloud segmentation labels, and trains deep learning models to detect and predict cloud motion over the Asia-Pacific region.

The central research question: **does temporal context improve cloud segmentation?** A U-Net operating on single frames is compared against a ConvLSTM that sees six consecutive frames, revealing that temporal information allows the model to substantially transcend the limitations of its own training labels.

---

## Results

| Model | Dice | IoU | Input |
|---|---|---|---|
| U-Net | 0.65 | 0.48 | Single frame |
| ConvLSTM | **0.95** | **0.93** | 6-frame sequence |

The ConvLSTM's strong performance — despite training on the same imperfect baseline labels as the U-Net — is the project's core finding. Temporal coherence across consecutive frames gives the model enough signal to reconstruct cloud boundaries that the label-generation algorithm missed entirely. The model learns not just *what* clouds look like but *how they move*, allowing it to fill in gaps that a purely spatial approach cannot recover.

---

## Engineering Challenges

### 1. Automated Data Ingestion at Scale

Himawari-9 produces a full-disk image of Earth every 10 minutes. This project ingests that data automatically via a GitHub Actions workflow (`ingest-hourly`) that runs every hour, downloading and stitching 4×4 PNG tiles from the NICT Himawari Real-time Web API, and saving 256×256 RGB composites to a dedicated `data` branch.

Over 60 days of continuous operation, the pipeline has collected **7,000+ frames** without manual intervention.

### 2. Production Outage Diagnosis and Recovery

Midway through data collection, the NICT tile server began returning 403 Forbidden responses for GitHub Actions runner IP ranges, halting ingestion entirely. Diagnosing the failure required distinguishing between rate limiting, IP blocking, and data source changes — ultimately confirmed by isolating the 403 pattern to specific GitHub-hosted runner IPs.

As a contingency, the ingestion module was partially rewritten to target JAXA's P-Tree FTP archive directly, requiring implementation of a custom HSD binary format parser (bz2 decompression → 16-bit count extraction → reflectance normalization → gamma correction). The block proved temporary — NICT's IP rotation resolved it — but the investigation produced a working understanding of the underlying satellite data format and a fallback ingestion path for future outages.

### 3. Label Quality and Threshold Sensitivity

Ground truth masks are generated by `cloud_mask_baseline_v2.py`, a tunable HSV-threshold algorithm. Extensive sensitivity analysis across luma thresholds (0.60 → 0.35) revealed that the default threshold systematically under-detected cloud at the disk limb due to oblique viewing geometry darkening cloud tops. A **limb-aware correction** was implemented that applies geometry-based adaptive thresholds in the outer 15% of the Earth disk, recovering cloud systems that would otherwise be absent from training labels entirely.

### 4. Multi-Branch Git Architecture

Code and data are maintained on separate git branches (`main` and `data`) to avoid inflating repository history with binary image files. GitHub Actions workflows operate across both branches — checking out `main` for code, merging into `data` for frame storage, and pushing outputs back without contaminating the code history. Day-specific output directories prevent cross-run contamination in training artifacts.

---

## Architecture

```
ingest-hourly (GitHub Actions, runs every hour)
    └── NICT Himawari Real-time Web → 4×4 tile stitch → 256×256 RGB → data branch

daily_rollup_phase2 (GitHub Actions, runs daily)
    ├── Phase 2: Storm tracking visualizations
    ├── Phase 3A: Baseline cloud mask generation (limb-aware HSV threshold)
    ├── Phase 3B: U-Net training (single-frame segmentation)
    └── Phase 4: ConvLSTM training (6-frame temporal forecasting)
```

---

## Repository Structure

```
himawari-ml/
├── .github/workflows/
│   ├── ingest-hourly.yml        # Hourly satellite data ingestion
│   └── daily_rollup_phase2.yml  # Daily training pipeline
├── src/himawari_ml/
│   ├── ingest/
│   │   └── fetch_latest.py      # NICT tile ingestion + stitch pipeline
│   ├── models/
│   │   ├── train_unet_pixelmask.py
│   │   └── train_convlstm.py
│   └── utils/
├── scripts/
│   ├── experiments/segmentation/
│   │   └── cloud_mask_baseline_v2.py   # Limb-aware cloud label generation
│   └── pipeline/
│       └── run_sample_pipeline.py
└── data/                        # data branch only
    └── raw/YYYY-MM-DD/latest/   # himawari_YYYYMMDDTHHMMSSZ.png
```

---

## Models

### U-Net (Baseline)
Single-frame semantic segmentation. Trained on 256×256 RGB frames with binary cloud masks generated by the HSV baseline algorithm. Performance is constrained by label quality and dataset size — a deliberate design choice to establish a realistic baseline.

### ConvLSTM (Primary)
Sequence-to-sequence model operating on 6-frame temporal windows (1 hour of satellite observations). Uses scheduled sampling during training to improve robustness at inference time. The substantial performance gap over U-Net (Dice +0.30, IoU +0.45) demonstrates that temporal coherence is a more powerful signal for cloud segmentation than spatial appearance alone.

---

## Setup

```bash
git clone https://github.com/evanjamison/himawari-ml
cd himawari-ml
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
pip install -e .
```

To run local baseline generation:
```bash
python scripts/experiments/segmentation/cloud_mask_baseline_v2.py \
  --raw-dir data/raw/sequence \
  --outdir test_out \
  --luma-thresh 0.35 \
  --sat-thresh 0.30 \
  --save-overlays \
  --limb-zone 0.85 \
  --limb-luma-offset 0.10
```

---

## Data

Frames are stored on the `data` branch under `data/raw/YYYY-MM-DD/latest/` and are not included in the main branch history. The ingestion workflow commits new frames hourly; the daily rollup consumes them for training.

Data source: [NICT Himawari Real-time Web](https://himawari8.nict.go.jp/) (open access, non-commercial research use).

---

## Limitations and Future Work

- **Label quality**: The HSV baseline captures dense cloud systems well but misses thin cirrus, which requires infrared channels. All metrics reflect agreement with the baseline definition rather than absolute meteorological accuracy.
- **Validation**: Cross-validation against JMA operational cloud products or MODIS observations would strengthen the scientific claims.
- **Ingestion latency**: NICT tiles are available within minutes of observation, making the pipeline suitable for near-real-time applications with minor scheduling adjustments.
